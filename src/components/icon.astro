---

import githubImg from '@/img/github-dark.png'
export interface Props {
  filename: string;
}

const { filename } = Astro.props as Props;
console.log(githubImg)
---
{
    (filename == "github") &&
    <img src={githubImg} height="32px"/>
}
