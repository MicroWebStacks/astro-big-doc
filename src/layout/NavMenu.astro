---
import './colors.css';
import SideMenu from './SideMenu.astro';
import { get_section_menu } from "./side_menu";

export interface Props {
	section: string;
}

const {section} = Astro.props;

const menu_items = await get_section_menu(section)
console.log("NavMenu>")
//console.log(menu_items)
---
<SideMenu items={menu_items}/>
